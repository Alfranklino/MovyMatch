const { gql } = require("apollo-server-express");

module.exports = gql`
  #   Query, Mutation, Subscription
  type Query {
    
  }

  type Mutation {
    
  }

  #   Main Customs Types

  type User {
    
  }

  type Movie_favourites {
    
  }

  # Main Inputs
  
`;
